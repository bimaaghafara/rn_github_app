const INITIAL_STATE = {
  username: 'uuuu',
  password: 'pppp',
  commits: [],
  nextCommits: [],
};

const reducers = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default reducers